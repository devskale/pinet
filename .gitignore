# Dependencies
node_modules/

# jot is an external repo
jot/

# Agent workspaces (local .pi/ settings + generated code)
frontend/
backend/
mastermaster/
tester/

# OS
.DS_Store

# Local pi config (symlinks, settings)
.pi/

# PiNet runtime data (lives in ~/.pinet/)
# Nothing to track here — state is per-machine

# Relay secret token (contains credentials)
relay-token
